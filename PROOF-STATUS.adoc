// SPDX-License-Identifier: CC-BY-SA-4.0
= tropical-resource-typing — Proof Status
:toc:

[abstract]
This repository is *dual-formalised*: Lean 4 and Isabelle/HOL. This document
records the *actually reproduced* verification state, distinguishing what was
type-checked in this environment from what is CI-gated but not re-checked here.

== Lean 4 — VERIFIED (this environment)

* Toolchain: **Lean 4 v4.13.0** (pinned in `lean-toolchain`), *no Mathlib*
  dependency (pure core Lean 4).
* Command: `lake build` after `rm -rf .lake/build` (clean rebuild).
* Result: **Build completed successfully — 20/20 targets**, i.e. every Lean
  module type-checks.

Modules (libs `Tropical` + `Resource`):

* `TropicalSessionTypes.lean`, `TropicalAdapterPath.lean` — the two
  order-reversing twins (max-plus session grading; min-max bottleneck transport).
* `Resource.lean` aggregator + `Resource/Algebra/{Interface,Ordered,Dioid,ParametricLaws}.lean`
  (the ordered-semiring / resource-algebra interface + parametric transport theorem).
* `Resource/Instances/{MaxPlus,MinPlus,MinMax,Linear,Affine}.lean` (concrete semirings).
* `Resource/{Bridge,Closure,Conformance,Stress,EchoBridge}.lean`.

Reproduce:
[source,sh]
----
# Lean 4 v4.13.0 (e.g. via elan, or the toolchain tarball from GitHub releases)
lake build
----

== Isabelle/HOL — present, CI-gated, NOT re-verified here

Nine theories form the `Tropical_Semirings` session: `Tropical.thy`,
`Tropical_v2.thy`, `Tropical_Matrices_{Clean,Full}.thy`, `Tropical_Determinants.thy`,
`Tropical_Kleene.thy`, `Tropical_Ordinal.thy`, `Tropical_Ordinal_Bridge.thy`,
`Tropical_CNO.thy`.

* CI-gated via `just isabelle-build` (the `isabelle build -D . -v Tropical_Semirings`
  session) + `just check-sorry`. Text inspection in this environment confirms
  **zero `sorry` and zero `oops` commands** across all 9 theories. (A Hypatia
  `isabelle_oops` finding on `Tropical_Ordinal.thy` is a *false positive*: it matches
  a descriptive **comment** at line 15 — "obligations marked with `oops` are the ones
  we want ECHIDNA to evaluate" — not an actual `oops` command; the obligations it
  describes were subsequently closed. Note `oops` ≠ `sorry`, so `check-sorry` alone
  would not catch an `oops` were one present.)
* **NOT re-verified in this environment** — Isabelle is a multi-GB toolchain not
  installed here. The Lean side above *was* reproduced; the Isabelle completeness
  rests on the in-file claims + CI + the marker text-scan above, not on an Isabelle
  run performed here.

== Honest scope

* The Lean result is a genuine, reproduced `lake build` (not a "looks fine"
  reading of in-file comments).
* The two formalisations are intended to agree on the tropical/resource-semiring
  laws (max-plus, min-plus, min-max, linear, affine) and the transport theorem;
  cross-prover *equivalence* is not mechanically established — each prover checks
  its own development.
* `Resource/EchoBridge.lean` is an echo-free residue-measure bridge (no dependency
  on the `echo-types` Agda kernel; the relationship is at the design level).
